import { DynamicComponentHelperDirective } from './dynamic-component-helper.directive';

describe('DynamicComponentHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicComponentHelperDirective();
    expect(directive).toBeTruthy();
  });
});
